# mhetwitter-node

## A Twitter API app.

### Setup
Dependencies:

Node.js
Python (requirements.txt)
Git (for git pull)
Pip (sudo apt-get install python-pip)+

collectionService.py run 1x per 24hrs.


